import React from "react";
import { Text } from "react-native-elements";
import { View, StyleSheet } from "react-native";

const TrackForm = () => {
  return (
    <View>
      <Text h5>Track Form</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default TrackForm;
